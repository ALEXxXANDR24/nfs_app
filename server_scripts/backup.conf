# /opt/backup/backup.conf

SOURCE_DIR="/data/students"

ONSITE_BACKUP_DIR="/backup/onsite"
ONSITE_KEEP_COPIES=3

OFFSITE_HOST=
OFFSITE_USER=
OFFSITE_DIR="/backup/offsite"
OFFSITE_SSH_KEY="/root/.ssh/backup_key"
OFFSITE_KEEP_COPIES=3

NFS_SERVICE="nfs-server"

ARCHIVE_PREFIX="backup"
DATE_FORMAT="%Y%m%d_%H%M%S"

RETRY_WAIT=300
RETRY_COUNT=3

LOG_DIR="/opt/backup/logs"
LOG_FILE="${LOG_DIR}/backup_$(date +%Y%m).log"

ADMIN_EMAIL=
